<!-- Footer -->
<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <span class="logo-text">CSS Playground</span>
      </div>
      <p class="footer-text">
        Creado con ❤️ para desarrolladores que aman el buen diseño - <a
          href="https://codigocreativo.netlify.app"
          target="_blank"
          rel="noopener noreferrer">Código Creativo</a
        >
      </p>
    </div>
  </div>
</footer>

<style>
  /* Footer */
  .footer {
    padding: 3rem 0;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    margin-top: 4rem;
  }

  .footer-content {
    text-align: center;
  }

  .footer-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: var(--font-size-lg);
    margin-bottom: 1rem;
  }
  .logo-text {
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .footer-text {
    color: var(--text-secondary);
    font-size: var(--font-size-sm);
  }
  .footer-text a {
    color: var(--text-secondary);
    transition: var(--transition-normal);
    cursor: pointer;
  }
  .footer-text a:hover {
    color: var(--text-primary);
    
  }
</style>
